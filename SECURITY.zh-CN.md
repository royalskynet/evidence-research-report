# 安全说明

## 运行边界

本技能由指令文本和一支仅使用 Python 标准库的验证器组成。它：

- 只读取用户明确提供的报告路径；
- 不写入文件；
- 不发起网络请求；
- 不读取环境变量、凭证、浏览器数据、智能体记忆或 shell 启动文件；
- 不执行子进程，也不安装任何依赖；
- 没有持久化、遥测、自动更新或二次下载机制。

验证器只在本地输出通过、警告或失败信息，并返回常规退出状态码。

## 安装前检查

应把每个技能视为可执行指令。安装前请检查压缩包清单、`SKILL.md` 与 `scripts/validate_report.py`。优先从固定提交或发布版本安装，并在提供校验值时进行比对。

## 报告数据

研究报告可能包含敏感商业信息。技能本身不会传送报告内容，但宿主智能体的浏览工具可能把搜索词或 URL 发送给其配置的服务商。不要把机密报告正文放进搜索词，并遵守所属组织的数据处理规则。

## 报告安全问题

请使用 GitHub Security Advisory，或提交不含秘密信息的最小化 issue。说明受影响版本、文件、可复现行为与预期安全边界。
