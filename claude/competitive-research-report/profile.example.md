# research skill — 委託方設定 Template
# 可公開的設定範本。填入實際值後另存為 profile.local.md（不對外發布）。

## 委託方身分

- 調用者：<調用者姓名或代號>
- 所屬：<組織／部門名稱>

## 報告署名

<署名規則，例如「寫上『[組織名稱] 風控部』」；若不需署名則填「不主動加註」>

## 考核維度（核心任務指令集）

制定建議時參考以下考核維度（由委託方指定；若委託方未提供則留空）：

- <考核維度 1，例如「損失控制指標」>
- <考核維度 2，例如「異常發現及時率」>
- <考核維度 3，例如「工具開發完成率」>
- <考核維度 4，例如「規範推進按時完成率」>

## 競品對標名單

<具名同業清單，由委託方指定；例如「平台 A、平台 B、平台 C」>

## 本地反偵測渲染通道

<可執行指令；介面須為 `<URL 或 @urls.txt>`，輸出 `URL / STATUS / TITLE / BODY`>

## 本地搜尋通道

<可執行指令；輸入為查詢字串，輸出須為 `TITLE<TAB>URL` 一行一筆>

## 單頁強韌抓取通道

<可執行指令；輸入單一 URL，輸出頁面正文；用於 WebFetch 失敗時的單頁補救>
